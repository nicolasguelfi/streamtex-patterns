[preset]
name = "minimal"
description = "Just the 3 atoms — for projects that only need basic emphasis primitives"

[patterns]
include = [
  "core/ptn_slide_heading.md",
  "core/ptn_cite.md",
  "core/ptn_inline_emphasis.md",
]
exclude = []

[depends_on]
extends = ""
