[preset]
name = "ai4se6d"
description = "Full set used by the ai4se6d training collection (slides preset + project-specific)"

[patterns]
include = [
  # everything from slides preset (extends below)
  # plus project-specific patterns:
  "projects/ai4se6d/ptn_transition_gse.md",
]
exclude = []

[depends_on]
extends = "slides"
