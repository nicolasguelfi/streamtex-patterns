# Project-specific patterns — ai4se6d

Patterns ultra-specific to the `ai4se6d` training collection that don't
fit into `core/`, `slides/` or `docs/`.

Currently: empty. Add patterns here only if they truly cannot be
generalized (e.g. they encode a project-specific palette assumption,
audience convention, or layout that other StreamTeX projects would not
reuse).

If a pattern initially put here proves universal, **promote** it to the
relevant scope (`core/`, `slides/`, ...) and update `presets/ai4se6d.toml`
to reference it from its new location.
